# AGENTS

## 项目约束

- 注释、显示文本和文档使用中文。
- 未经许可，不在 UI 中添加提示词辅助文本。
- 单文件不超过 500 行，超出时按逻辑拆分。
- 每个静态配置项、函数、接口和方法声明前写一行简短注释。
- TODO.md 只在用户明确授权规划时增删任务；正常开发只勾选已验收项目。
- 默认不使用子代理；只有用户明确要求“使用子代理”或“MULTI_RUN”才启用。

## 分支与发布

- 所有日常开发、修复、依赖更新和版本号调整直接在 `dev` 分支进行，不创建其他开发分支。
- 动手前检查当前分支和工作区；保留现有未提交内容，禁止为切换分支丢弃改动。
- `main` 只接收同仓库 `dev → main` 的合并请求，不直接开发或提交。
- 合并前必须通过 `CI / verify`；main 分支保护必须禁止绕过门禁、强推和删除。
- 正式版本标签使用 `vX.Y.Z`，必须与根 package.json 的 version 一致，指向已进入 main 的提交。
- 单独推送 main 只检查，不发布；推送合法版本标签才创建 GitHub Release。
- 已发布标签和资产不得移动、删除或覆盖；修复通过 dev 开发并发布新版本。
- 未经明确要求，不主动推送、打标签或发布。创建工作流不等于授权立即发布当前版本。

## 测试约定

- `bun run test` 运行隔离的行为测试；数据库使用内存或临时目录，Git 测试使用临时仓库。
- 新增业务规则必须覆盖成功、无权限、无效输入和相关状态变化；修复 Bug 增加能复现问题的回归测试。
- 认证、事务、迁移、并发写入和发布校验的改动不能只用类型检查代替测试。
- 禁止测试读取真实 `.env` 凭据、访问私人数据库或调用真实外部服务。
- 禁止为了通过 CI 删除断言、跳过失败测试、扩大忽略范围或使用 continue-on-error。
- 没有实际执行测试时明确说明，不能把静态检查当作运行验证。

## 自动构建约定

- 本地和 CI 统一执行 `bun run check`：lint、全部类型检查、行为测试、生产构建、产物联调。
- CI 固定 Bun 1.4.2，并使用 `bun install --frozen-lockfile`；依赖变更同步提交 bun.lock。
- dev push、main push 及面向这两个分支的 PR 均执行完整门禁，不通过路径过滤跳过检查。
- Release 在标签提交上重新跑完整门禁；只打包本次验证后的产物。
- 发布包保留目录层级，包含账号初始化命令、版本与提交信息、SHA-256 校验；禁止包含 `.env`、数据、缓存或开发依赖。
- 工作流外部 Action 固定完整提交 SHA，更新时同时核查版本；普通检查使用只读权限。
- 工作流改动使用 actionlint 校验；脚本改动纳入类型检查和相关测试。
- 构建或测试失败时停止交付，不宣布完成；远程运行未验证时明确区分本地结果。

## 文档入口

- 产品方向：`docs/DESIGN.md`、`docs/REQUIREMENTS.md`
- 基础架构：`docs/ARCHITECTURE.md`
- API：`docs/API.md`
- 开发与发布规则：`docs/CI.md`
- 项目介绍与文档索引：`README.md`
- 安装、启动和构建步骤：`docs/QUICKSTART.md`，不写进 README
- 实施计划：`TODO.md`
